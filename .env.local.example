# Free Jamendo API client id — get one at https://devportal.jamendo.com/
# Safe to use directly in the browser (rate-limited public key, not a secret).
# Without it, music search is unavailable but local audio upload still works.
NEXT_PUBLIC_JAMENDO_CLIENT_ID=
